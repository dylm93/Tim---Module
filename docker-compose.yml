version: '2'
services:
  web:
    container_name: dylan-docker
    build: .
    ports: 
    - "80:3000"
    links:
    - postgres
  postgres: 
    container_name: postgres
    image: postgres
    ports:
    - '5432:5432'
